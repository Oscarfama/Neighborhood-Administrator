export class Visitor {
  FirstName: string;
  LastName: string;
  HouseStreet:string;
  HouseNumber: string;
  VehicleModel?: string;
  VehicleNumber: string;
}
tring;

  constructor(obj?: any)
  {
    obj && Object.assign(this, obj);
  }

}
